__pycache__/
*.pyc
run-logs/*
!run-logs/.gitkeep
# Legacy npm cache dir (kept ignored so pre-existing host folders from the
# old Gemini CLI install do not show as untracked; safe to delete by hand).
npm-cache/
.venv/
.env
.env.*
!.env.example
docker-compose.override.yml
# Host-specific override selecting a digest-pinned release image. Names a
# registry and a deployment-specific digest, so it stays out of the public repo.
docker-compose.release.yml
config/projects.local.yaml
config/routes.local.yaml
*.egg-info/
*.log
node_modules/
.idea/
.vscode/
.mypy_cache/
.claude/
.antigravitycli/
.playwright-mcp/
projects/
